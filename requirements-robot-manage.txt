# Layer 2/2 for robot_manage (FastAPI / uvicorn / httpx).
# Install: pip install -r requirements.txt -r requirements-robot-manage.txt
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
httpx>=0.27.0
gTTS>=2.5.1
mutagen>=1.47.0
# Apple Silicon MLX stack: install ``mlx-metal`` explicitly so ``mlx/lib/libmlx.dylib`` is never
# missing (partial upgrades / conflicting wheels can leave only ``libjaccl.dylib``).
mlx-metal>=0.31.2; sys_platform == "darwin"
mlx>=0.31.2; sys_platform == "darwin"
mlx-whisper>=0.4.3; sys_platform == "darwin"
