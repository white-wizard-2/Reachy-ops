# Apple Silicon: MLX Whisper ASR (optional extra pins / experiments).
# ``requirements-robot-manage.txt`` already installs ``mlx-whisper`` on macOS (``sys_platform == "darwin"``).
# ``run_robot_manage.sh`` still installs this file when present.
mlx-whisper>=0.4.3
